<!DOCTYPE html>
<html lang="en">

<head>
    <title>Thay đổi thông tin cá nhân</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Arbutus" rel="stylesheet">
</head>
<link href="../../css/style.css" rel="stylesheet" />

<body>

    <div class="container">
        <form action="./edit" class="form-login " method="POST" autocomplete="on" enctype="multipart/form-data">
            <input name="token" value="<%= data.token %>" style="display: none;" type="text">
            <img src="./../../<%= data.avatar%>" alt="Avatar" class="avatar">
            <div class="upload-image-student">
                <div class="upload-btn-wrapper">
                    <button class="btns">Chọn ảnh</button>
                    <input type="file" name="avatar" accept="image/png, image/gif, image/jpeg" />
                </div>
            </div>
            <hr>
            <div class="profiledata ">
                <h5 class="profiledata_1 label_editprofile_phongkhoa">Tên : </h5>
                <input class="profiledata_1 input_editprofile_phongkhoa" type=" text " value="<%= data.name%>" id="" name="name"><br><br>
            </div>
            <div class="profiledata ">
                <h5 class="profiledata_1 label_editprofile_phongkhoa">Lớp : </h5>
                <input class="profiledata_1 input_editprofile_phongkhoa" type=" text " value="<%= data.class%>" id="" name="Class"><br><br>
            </div>
            <div class="profiledata ">
                <h5 class="profiledata_1 label_editprofile_phongkhoa">Khoa : </h5>
                <input class="profiledata_1 input_editprofile_phongkhoa" type=" text " value="<%= data.faculty%>" id="" name="faculty"><br><br>
            </div>
            <div class="form-group ">
                <button type="submit " class="btn btn-info btn-editprofile ">Xác nhận thay đổi</button>
            </div>
            <br>
            <br>
            <hr>
            <% if(locals.flash.success && flash.success.length > 0) { %>
                <% if (flash.success == -1){ %>
                    <div class="alert alert-danger">
                    <strong>Error! </strong>
                <% } else { %>
                    <div class="alert alert-success">
                    <strong>Success! </strong> 
                <% } %>
                <%= flash.msg %>.
                </div>
            <% } %>


        </form>

    </div>

</body>

</html>